// =============================================================================
// Reago constants
// =============================================================================

export const METADATA: unique symbol = Symbol();

export const NO_VALUE: unique symbol = Symbol();

export const FUNCTIONAL_ATOM: unique symbol = Symbol();
export const GENERATIVE_ATOM: unique symbol = Symbol();

export const PENDING: unique symbol = Symbol();
export const RESOLVED: unique symbol = Symbol();
export const REJECTED: unique symbol = Symbol();

export const LOADED: unique symbol = Symbol();
export const UNLOADED: unique symbol = Symbol();

export const COMPUTED: unique symbol = Symbol();
export const COMPUTING: unique symbol = Symbol();
export const OUTDATED: unique symbol = Symbol();

export const FRESH: unique symbol = Symbol();
export const STALE: unique symbol = Symbol();

export const MOUNTED_DIRECTLY: unique symbol = Symbol();
export const MOUNTED_TRANSITIVELY: unique symbol = Symbol();
export const UNMOUNTED: unique symbol = Symbol();
